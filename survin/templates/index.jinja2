<!DOCTYPE html>
<html>

<head>
    <title>Videos</title>
    <link rel="stylesheet" href="static/styles.css">
    <script src="https://unpkg.com/htmx.org@1.9.12"></script>
</head>

<body>
    {% for file_path, classifications in files %}
    <div class="video-container">
        <div class="video-info">
            {{file_path}}: {{", ".join(classifications)}}
        </div>
        <div>
            <img class="video" src="snapshot/{{file_path}}" width="600" hx-get="videoplayer/{{file_path}}"
                hx-swap="outerHTML" />
        </div>
    </div>
    {% endfor %}


    <button hx-get="/hello" hx-swap="outerHTML">
        Click Me
    </button>
</body>
